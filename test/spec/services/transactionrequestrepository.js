'use strict';

describe('Service: TransactionRequestRepository', function () {

  // load the service's module
  beforeEach(module('orphaApp'));

  // instantiate service
  var TransactionRequestRepository;
  beforeEach(inject(function (_TransactionRequestRepository_) {
    TransactionRequestRepository = _TransactionRequestRepository_;
  }));


});
